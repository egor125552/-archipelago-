# Archipelago portable Windows shell

This folder builds a single portable `Archipelago.exe` that opens the live Archipelago web game. The application icon is generated from the lighthouse artwork source during CI. Normal game updates do not require rebuilding the executable because the shell loads the live deployed game URL.

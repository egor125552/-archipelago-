"use strict";

// Replaced by the browser training laboratory. Keeping the weights in a
// separate module makes the neural controller removable without touching
// the boat physics or obstacle avoidance code.
// Browser rerun: isolate the sustained reverse-control scenario.
export const HUNTER_BRAIN_WEIGHTS = null;

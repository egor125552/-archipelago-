# Эхо-Архипелаг

Доступная браузерная кооперативная аудио-игра про спасение на лодке.

## Что работает

- Управление лодкой с инерцией в тихой речной бухте без постоянного течения.
- Отдельный режим VoiceOver без двойных команд и без скачущего фокуса.
- Пространственный сонар, двигатель, перегрев, повреждения, течь, насос и спасательный трос.
- Одиночная игра с помощником и кооператив капитан/оператор.
- Раздельный звуковой слой стоянки, кильватера и воды внутри корпуса.
- При полном затоплении остаётся 32 секунды на аварийный ремонт и откачку.
- Озвучка игровых сообщений русским голосом со скоростью `1.18`; в режиме VoiceOver встроенная речь по умолчанию выключена.
- Кнопка «Начать новую операцию» сбрасывает таймер, сеть, звук и игровое состояние без перезагрузки страницы.

## Звуки и лицензии

Речная вода и внутренний плеск используют записи из проекта Veloren:

- `running_water-004.ogg`
- `running_water-019.ogg`
- `running_water-026.ogg`

Авторы/редактура: DaforLynx. Лицензия: CC BY 4.0. Исходные записи перечислены в `assets/credits.ron` репозитория Veloren.

Остальные сценарные сигналы и часть механических эффектов используют ранее подключённые CC0/совместимые записи. Сценарные звуки сонара, поворота и спасения не заменялись.

## Локальный запуск

```bash
npm install
npm test
npm run serve
```

Открой `http://localhost:4173/`.

## Cloudflare Workers Static Assets

```bash
npm install
npx wrangler login
npm run deploy
```

Для автоматического деплоя через Cloudflare Builds:

- Repository: `egor125552/-archipelago-`
- Production branch: `main`
- Root directory: оставить пустым
- Build command: `npm install && npm test`
- Deploy command: `npx wrangler deploy`

После подключения каждый коммит в `main` будет запускать проверку и публикацию.

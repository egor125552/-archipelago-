# Продолжение интервью по архитектуре судов

Это продолжение живого журнала `docs/VESSEL_ARCHITECTURE_INTERVIEW.md`. Интервью по-прежнему не завершено; текущая архитектура остаётся прототипом. Правило сохраняется: один вопрос за раз, затем ответ Егора, вывод GPT и только подтверждённое решение.

---

## Интервью 11. Отдельное место установки и сменный модуль

### Вопрос GPT

Представь корабль, на носу которого стоит обычная пушка. Нужно ли в будущем иметь возможность снять эту пушку и поставить на её место другую, например электрошоковую — то есть чтобы отдельно существовало место установки, а сама пушка была сменным модулем?

### Ответ Егора

«Ну, пусть будет. Пусть это будет.»

### Вариант / вывод GPT

Архитектура должна разделять **место установки** и **установленный модуль**. Судно описывает допустимое место/точку крепления, а конкретное оборудование является отдельным экземпляром модуля, который может быть установлен, снят или заменён без переписывания definition самого судна и без добавления специальной логики в общий игровой цикл.

Это не означает, что прямо сейчас нужно реализовывать магазин замены оборудования или физическую анимацию снятия. Сейчас фиксируется только архитектурная граница, чтобы такие механики можно было добавить позже.

### Решение

Подтверждено:

1. Место установки и установленное оборудование должны быть разными сущностями архитектуры.
2. В будущем модуль может быть заменён другим совместимым модулем в том же месте установки.
3. Само наличие сменных модулей не требует заранее реализовывать конкретную механику магазина/снятия/установки — это проектируется тогда, когда такая игровая функция реально понадобится.

---

## Интервью 12. Совместимость места установки и необычных модулей

### Вопрос GPT

Должна ли архитектура по умолчанию запрещать ставить несовместимый модуль в неподходящее место установки — например двигатель в обычное место под пушку — но позволять явно разрешить нестандартную совместимость для специально задуманного необычного модуля?

### Ответ Егора

«Да. Вот так сделаем.»

### Вариант / вывод GPT

Совместимость должна быть явной частью контракта между местом установки и модулем. Обычные комбинации проходят автоматически по объявленной совместимости. Случайная несовместимая комбинация должна считаться ошибкой архитектуры и блокироваться строгой проверкой.

При этом архитектура не должна запрещать необычные игровые идеи навсегда. Если специально создаётся гибридный или нестандартный модуль, его дополнительная совместимость объявляется явно в definition/contract и после этого проходит тот же валидатор. Таким образом правило означает не «нестандартное запрещено», а «нестандартное нельзя получить случайно или скрытым обходом».

### Решение

Подтверждено:

1. По умолчанию модуль нельзя установить в несовместимое место.
2. Несовместимая установка должна блокироваться архитектурным build gate, а не оставаться предупреждением.
3. Для специально задуманного необычного модуля можно явно объявить дополнительную/нестандартную совместимость.
4. Явное расширение совместимости проходит тот же общий контракт и не является обходом архитектуры.

---

## Интервью 13. Один модуль может занимать несколько мест установки

### Вопрос GPT

Если появляется очень крупный модуль — например тяжёлая турель, которой одного места установки недостаточно, — должна ли архитектура позволять одному модулю занимать сразу несколько мест на судне?

### Ответ Егора

Сначала Егор ответил: «Ну типа, ну можно сделать. Ну да. Согласен.» После уточнения, зачем вообще нужны несколько мест и не является ли это лишней сложностью, Егор подтвердил решение: «GPT шка, ну пусть будет, давай для архитектуры мы это зафиксируем».

### Вариант / вывод GPT

Контракт установки не должен предполагать жёсткое правило «один модуль = одно место». Один экземпляр модуля может объявить, что для установки ему требуется несколько совместимых мест/креплений. Эти места остаются отдельными сущностями судна, но могут быть заняты одной общей установкой.

Это позволяет в будущем выражать тяжёлые турели, крупные генераторы, составные двигательные установки или другие большие системы без специального кода конкретного корабля. При этом сейчас не требуется реализовывать конкретные такие модули: фиксируется только возможность контракта.

### Решение

Подтверждено:

1. Один модуль может занимать больше одного места установки.
2. Поддержка нескольких мест является общей возможностью mount/module-контракта, а не исключением для конкретной тяжёлой турели.
3. Это архитектурная возможность на будущее; конкретные многоместные модули сейчас создавать не требуется.

---

## Интервью 14. Количество требуемых мест задаётся самим модулем

### Вопрос Егора

Егор уточнил: «А мы типа можем указывать, то есть два ли места надо, или одно, то есть мы можем это указывать если что да.»

### Вариант / вывод GPT

Да. Количество необходимых мест установки не должно быть глобальным свойством всей архитектуры. Каждый тип модуля сам объявляет свои требования к установке: одному модулю может требоваться одно совместимое место, другому — два, а будущему крупному модулю при необходимости больше.

Валидатор должен читать требования конкретного модуля и проверять, что судно предоставляет нужное количество совместимых мест. Это позволяет расширять размеры/требования модулей без специальных условий по их именам в общем коде.

### Решение

Подтверждено:

1. Сам модуль задаёт, сколько мест установки ему требуется.
2. Архитектура не фиксирует единое число мест для всех модулей.
3. Валидатор должен проверять требования конкретного модуля к количеству и совместимости мест.
4. Это описание является частью module/mount-контракта и не требует специальной ветки для конкретного типа оборудования.

---

## Интервью 15. Многоместный модуль устанавливается атомарно

### Вопрос GPT

Если модулю указано, что нужны два места, можно ли считать его установленным только когда оба места заняты им полностью, или допускается промежуточное состояние, когда занято только одно из двух мест?

### Ответ Егора

«Но если два места, то тогда полностью пусть будет занят. Но а зачем он промежуточно установлен.»

### Вариант / вывод GPT

Для архитектурного контракта многоместная установка должна быть атомарной. До начала установки валидатор/система проверяет сразу весь набор требуемых мест. Если хотя бы одно место отсутствует, занято или несовместимо, установка не начинается и состояние судна не меняется частично.

Если все требуемые места доступны, один экземпляр модуля занимает их одновременно. Аналогично базовая операция снятия должна освобождать весь набор мест целиком. Это не запрещает когда-нибудь отдельно спроектировать игровую механику длительного монтажа, но промежуточный процесс монтажа тогда должен быть отдельным состоянием операции, а не считаться частично установленным рабочим модулем.

### Решение

Подтверждено:

1. Многоместный модуль считается установленным только целиком.
2. Все требуемые места должны быть доступны и совместимы до изменения установленной конфигурации.
3. Частично установленный рабочий модуль не является допустимым состоянием vessel/module-контракта.
4. Если в будущем появится длительный монтаж, он проектируется отдельно как процесс, не нарушая атомарность итоговой установленной конфигурации.

---

## Интервью 16. Автоматическая миграция старых сохранений

### Вопрос GPT

Если у игрока есть старое сохранение с лодкой, а позже эта лодка переводится на новую vessel-архитектуру, должно ли сохранение автоматически мигрироваться в новый формат при загрузке, без требования начинать новый мир?

### Ответ Егора

«Конечно мигрировала. Конечно. Естественно.»

### Вариант / вывод GPT

Формат сохранений должен быть версионирован. При загрузке старого мира система определяет версию сохранённых vessel/boat-данных и последовательно применяет зарегистрированные миграции до текущей версии. Миграция должна преобразовывать данные, а не заставлять старую игровую механику навсегда жить рядом с новой.

Для перехода нынешних лодок в native-vessel формат мигратор должен переносить только фактически существующее состояние: идентичность экземпляра, владельца/экипаж, позицию, курс, скорость, корпус/броню, груз, топливо, повреждения и другие реально присутствующие данные. Он не должен выдумывать состояние будущих модулей, которых в старой версии не существовало; для новых обязательных полей используются только явно определённые безопасные defaults конкретной schema migration.

### Решение

Подтверждено:

1. Старые сохранённые миры не требуют начинать заново после перехода лодок на новую vessel-архитектуру.
2. Миграция старого формата в новый должна выполняться автоматически при загрузке.
3. Пользователь не должен вручную конвертировать сохранение или повторно создавать судно.
4. Миграция должна быть частью общего versioned save/vessel contract, а не специальным одноразовым костылём в конкретной лодке.
5. Конкретные правила переноса каждого будущего нового поля должны задаваться явной migration-версией, а не угадываться во время загрузки.

---

## Интервью 17. Миграция должна завершаться без потери данных

### Вопрос GPT

Если старое сохранение повреждено или миграция не смогла корректно перенести лодку, нужно ли пытаться загрузить мир частично, сбрасывая часть данных, либо сохранять исходное состояние и не принимать неполную миграцию?

### Ответ Егора

Егор не стал выбирать аварийный сценарий как игровую политику и сформулировал ожидаемый результат: «Ну мы по идее доделаем... я в любом случае тебя буду просить, чтоб ты доделал так что вот.» То есть миграция должна быть реализована полноценно, а не рассчитывать на частичный перенос как нормальное состояние.

### Вариант / вывод GPT

Техническая реализация выбирается по критерию сохранности данных. Нормальный путь — миграция обязана полностью преобразовать поддерживаемую старую схему в текущую. Частичная миграция с молчаливым сбросом неизвестных/неперенесённых данных не считается допустимым успешным результатом.

Для непредвиденного аварийного случая безопаснее не перезаписывать исходное сохранение. Система сначала мигрирует данные в отдельное временное представление, валидирует полный результат и только после успешной проверки принимает новую версию. Если преобразование невозможно, исходная сохранённая версия остаётся нетронутой, а загрузка завершается явной recoverable-ошибкой вместо скрытой потери состояния.

### Решение

Подтверждено на уровне требуемого результата:

1. Поддерживаемая миграция должна быть доделана так, чтобы старый мир переносился полностью, а не «как получится».
2. Потеря части состояния лодки не является нормальным допустимым способом завершить миграцию.

Техническая страховка выбрана GPT по критерию надёжности и сохранности данных: исходное сохранение не перезаписывается до полной успешной миграции и валидации; при невозможности преобразования лучше остановить загрузку с явной ошибкой, чем молча уничтожить часть данных. Это не отдельная игровая политика, а fail-safe реализация требования Егора о полноценной миграции.

---

## Интервью 18. Локальная позиция игрока внутри большого судна и именованные ориентиры

### Вопрос GPT

Для будущего большого корабля, который продолжает двигаться по миру, пока игрок ходит по палубе и внутренним помещениям, как лучше представлять положение игрока внутри самого судна?

### Ответ Егора

Егор попросил выбрать «самый топовый вариант» и привёл желаемый пользовательский результат: «Ну, пусть оно говорит типа, в 8 м от машинного отделения... Как же сделать самый топовый вариант. Для судов.»

### Вариант / вывод GPT

Для больших судов используется собственная локальная система координат, привязанная к судну. Игрок хранится как находящийся на конкретном `vessel`, в конкретной логической зоне и с локальной позицией внутри координатной системы этого судна. Мировая позиция игрока не является главным внутренним состоянием: она вычисляется из мирового положения/курса судна и локальной позиции игрока.

В definition большого судна дополнительно существуют именованные ориентиры/anchors и зоны: например «машинное отделение», «мостик», «левая турель», «носовая палуба», вход в отсек и т. п. Ориентир имеет локальное положение или геометрию внутри судна и доступное человеку название.

Фразы вроде «машинное отделение — 8 метров впереди» не хранятся как состояние. Навигационный/presentation слой вычисляет расстояние и направление от текущей локальной позиции игрока до выбранного ориентира и формирует семантическое сообщение. Поэтому при движении и повороте корабля игрок остаётся в правильном месте относительно палубы, а внутренняя навигация не ломается.

Маленькие лодки не обязаны нести на себе сложную внутреннюю геометрию. Для них этот слой может отсутствовать или сводиться к простой зоне/точке, тогда как большие walkable-суда используют полный `vessel + zone + localPosition + landmarks` контракт.

### Решение

Подтверждено направление для больших судов:

1. Большое судно имеет собственную локальную систему координат для внутреннего пространства.
2. Игрок внутри него привязан к конкретному судну и хранит локальную позицию; дополнительно может храниться текущая зона/отсек.
3. Важные места судна описываются именованными ориентирами/зонами с локальным положением.
4. Расстояние и направление до машинного отделения, мостика, турели и других ориентиров вычисляются динамически из локальных координат и затем озвучиваются/показываются через общий presentation layer.
5. Формулировка наподобие «машинное отделение — 8 метров впереди» является производным пользовательским сообщением, а не сохранённым координатным состоянием.
6. Движение/поворот самого корабля не должно требовать вручную переписывать мировые координаты каждого находящегося внутри игрока как основной способ представления.
7. Малые лодки могут использовать упрощённый вариант и не обязаны иметь сложное внутреннее пространство только ради общего контракта.

---

## Интервью 19. Несколько палуб, настраиваемая геометрия и пресеты

### Вопрос GPT

Должно ли большое судно поддерживать несколько палуб/уровней — например нижнюю палубу, главную палубу и мостик — с переходами между ними по лестницам, люкам и другим точкам перехода?

### Ответ Егора

«Да, давай. Давай так сделаем. Вот. И чтоб можно было прям размеры вот это все настраивать полностью, либо готовые пресеты выбрать, если мы их создадим.»

### Вариант / вывод GPT

Внутреннее пространство большого судна не должно быть ограничено одной плоскостью. Definition может содержать произвольное количество палуб/уровней. Каждая палуба является частью общей локальной системы координат судна и может содержать свои зоны, помещения, ориентиры, места установки и точки перехода на другие палубы. Переход между уровнями описывается общим переходом/connection, а не специальным кодом конкретной лестницы или конкретного корабля.

Размеры и геометрические параметры должны быть данными definition, а не жёстко зашитыми в код. Для нового судна допускаются два равноправных пути описания: полностью явная конфигурация размеров/палуб/пространств или готовый пресет, если подходящий пресет существует. Пресет после разворачивания превращается в обычное каноническое definition и проходит тот же валидатор; отдельной «облегчённой» архитектуры для пресетов нет. Разрешены явные переопределения параметров пресета там, где это допускает его контракт.

Это не означает, что сейчас нужно создать конкретный набор больших корабельных пресетов или готовую систему редактора геометрии. Сейчас фиксируется архитектурная возможность: полный ручной контроль и возможность удобных пресетов поверх того же контракта.

### Решение

Подтверждено:

1. Большие суда могут иметь несколько палуб/уровней.
2. Игрок может переходить между палубами через явно описанные точки/связи, например лестницы и люки.
3. Количество палуб и их размеры не должны быть жёстко зашиты в общий код.
4. Размеры и геометрические параметры судна должны настраиваться в его definition.
5. Можно описать судно полностью вручную либо использовать готовый пресет, если такой пресет создан.
6. Пресет разворачивается в тот же канонический definition и проходит те же строгие проверки, что и полностью явное описание.
7. Наличие поддержки пресетов не обязывает заранее создавать конкретные пресеты, пока они реально не нужны.

---

## Интервью 20. Произвольная форма палуб и внутренних пространств

### Вопрос GPT

Если размеры судна настраиваются полностью, должна ли палуба быть ограничена простой прямоугольной формой, либо архитектура должна позволять описывать более реальную форму: сужающийся нос, широкую корму, отдельные комнаты, коридоры, вырезы и другие контуры?

### Ответ Егора

«Ну не знаю. GPT шка. Наверное да.»

### Вариант / вывод GPT

Для долгоживущей архитектуры не стоит делать прямоугольник фундаментальным ограничением. Definition большого walkable-судна должен позволять задавать произвольную двумерную геометрию палуб/зон в локальной системе координат: контуры, помещения, коридоры и вырезы. Прямоугольник остаётся допустимым простым частным случаем и может использоваться в простых пресетах.

При этом это решение не требует прямо сейчас создавать визуальный редактор полигонов или сложную геометрию для существующих лодок. Контракт лишь не должен закрывать такую возможность. Конкретный формат представления контура можно выбрать при реализации так, чтобы он был валидируемым и пригодным для навигации, столкновений и доступного описания ориентиров.

### Решение

Зафиксировано как выбранное направление:

1. Палубы и внутренние пространства больших судов не ограничиваются прямоугольниками.
2. Архитектура должна позволять описывать произвольный локальный контур и разбивку на помещения/коридоры/зоны.
3. Простые прямоугольные формы остаются полностью допустимыми и удобными для пресетов.
4. Точный низкоуровневый формат геометрии не фиксируется интервью заранее; он выбирается при реализации при условии, что не ломает локальную навигацию, коллизии и строгую валидацию.
5. Создавать сложный редактор геометрии или готовые сложные корабли сейчас не требуется.

---

## Интервью 21. Изолированный пользовательский физический модуль судна

### Вопрос GPT

Если для очень необычного судна обычных параметров `physicsProfile` недостаточно — например оно движется или поворачивает принципиально иначе, — должна ли архитектура позволять подключить отдельный физический модуль, не переписывая общий движок остальных лодок?

### Ответ Егора

«Да, но тогда чтобы наверно только он не использовался. Или как-то ещё.»

### Вариант / вывод GPT

Базовая физика остаётся общей и используется всеми судами по умолчанию. Если конкретному типу судна требуется принципиально иное поведение, его definition может явно сослаться на зарегистрированный `physics module`/physics strategy. Такой модуль выполняется только для экземпляров судов, которые явно выбрали его, и не меняет глобальные правила движения остальных лодок.

При этом модуль не обязан быть навсегда привязан к одному названию корабля. Если в будущем другое судно намеренно выберет тот же физический модуль и удовлетворит его контракту, поведение можно безопасно переиспользовать без копирования кода. Изоляция означает отсутствие неявного глобального влияния, а не запрет повторного использования.

Если новое поведение укладывается в существующий `physicsProfile`, достаточно данных/параметров и новый код не нужен. Отдельный физический модуль создаётся только для действительно нового закона движения/управления.

### Решение

Подтверждено:

1. По умолчанию суда используют общий физический механизм/профиль.
2. Судно может явно выбрать отдельный зарегистрированный физический модуль, если обычных параметров недостаточно.
3. Пользовательский физический модуль влияет только на суда, которые явно его подключили; он не должен неявно изменять физику остальных лодок.
4. Один и тот же физический модуль может быть намеренно переиспользован несколькими типами судов, если они явно на него ссылаются.
5. Новая вариация параметров остаётся конфигурацией `physicsProfile`; отдельный модуль нужен только для действительно нового физического поведения.
6. В общем игровом цикле не должно появляться веток по имени конкретного судна ради выбора его физики.

---

## Интервью 22. Контрольный третий катер — только по отдельной просьбе

### Вопрос GPT

Когда базовая архитектура будет готова, стоит ли сделать третий простой тестовый катер, собранный только из уже существующих модулей, чтобы проверить, что новый тип судна добавляется без переписывания общего игрового цикла, сонара, ремонта, аудио и других общих систем?

### Ответ Егора

«Можем да, но только когда я тебя прошу. Да я согласен».

### Вариант / вывод GPT

Третий контрольный катер полезен как приёмочный тест архитектуры: его definition должен собираться из уже существующих возможностей платформы и тем самым доказывать, что добавление нового типа судна не требует новых веток по имени конкретного катера в общих системах.

Однако это не является разрешением создавать такой катер сейчас. Архитектурная проверка и фактическое добавление игрового контента — разные действия. Создание контрольного катера выполняется только после отдельной явной просьбы Егора.

### Решение

Подтверждено:

1. Контрольный третий катер допускается как будущая приёмочная проверка vessel-архитектуры.
2. Он должен по возможности состоять из уже существующих модулей/возможностей, чтобы проверять расширяемость без изменений общих систем.
3. Сам факт этого решения не разрешает добавлять катер в игру сейчас.
4. Создавать, подключать или публиковать такой катер можно только после отдельной явной просьбы Егора.
5. До такой просьбы интервью и журнал не должны сами инициировать изменение игрового контента.

---

## Интервью 23. Миссионная навигация имеет приоритет над ручным фильтром сонара

### Вопрос GPT

Если в настройках игрок скрыл какое-то судно из ручного списка целей сонара, но сюжет или миссия требует привести игрока именно к этому судну, должна ли миссионная навигация всё равно иметь право показать его и вести к нему?

### Ответ Егора

«GPT, да. Если сюжетная миссия, то да. Пусть так и будет».

### Вариант / вывод GPT

Нужно разделять пользовательскую фильтрацию обычных ручных целей и обязательную навигационную информацию активной миссии. Настройка игрока управляет тем, какие необязательные суда появляются в обычном ручном списке целей. Она не должна делать сюжетную задачу непроходимой.

Если активная миссия явно требует конкретное судно как цель/ориентир, mission/navigation слой может временно предоставить эту цель независимо от ручного фильтра. Это не означает постоянного изменения пользовательской настройки: вне миссионного контекста судно остаётся скрытым так, как выбрал игрок.

### Решение

Подтверждено:

1. Ручной фильтр сонара применяется к обычным необязательным целям.
2. Активная сюжетная/миссионная навигация может показать и использовать судно, скрытое ручным фильтром, если оно необходимо для задания.
3. Такое миссионное исключение не должно молча менять сохранённую пользовательскую настройку фильтра.
4. После исчезновения миссионной необходимости обычный ручной список снова следует пользовательской настройке.
5. Миссионный override должен быть общей возможностью navigation contract, а не специальной проверкой конкретного судна.

---

## Интервью 24. Режим миграции старых лодок выбирается под задачу

### Вопрос GPT

Когда нынешние старые лодки будут переводиться на новую vessel-архитектуру, нужно ли жёстко требовать миграцию только по одной лодке за раз с полной проверкой каждой, либо допускаются и другие режимы переноса?

### Ответ Егора

«Можем и так и так, ну как мне захочется. Вот.»

### Вариант / вывод GPT

Архитектура не должна превращать безопасную поэтапную миграцию в единственный разрешённый процесс разработки. По умолчанию перенос по одной лодке удобен тем, что уменьшает область риска и позволяет отдельно проверять сохранение старого поведения. Но если Егор явно выбирает перенос нескольких лодок одновременно, это также допустимо.

Независимо от выбранного режима требования к результату одинаковы: существующее игровое поведение не должно самопроизвольно меняться, старые сохранения должны мигрироваться по versioned contract, новые native-vessel definitions должны проходить те же строгие архитектурные проверки, а общий игровой цикл не должен обрастать новыми ветками по конкретным типам лодок.

### Решение

Подтверждено:

1. Допустима поэтапная миграция старых лодок по одной с проверкой после каждого шага.
2. Допустима миграция нескольких существующих лодок за один этап, если Егор отдельно выбирает такой объём работы.
3. Выбор режима миграции является решением конкретной задачи/итерации, а не жёстким ограничением vessel-архитектуры.
4. В обоих режимах обязательны сохранение существующего поведения, корректная миграция сохранений и прохождение строгих архитектурных проверок.
5. Само это решение не является разрешением начинать миграцию существующих лодок без отдельной просьбы Егора.

---

## Интервью 25. Компактная репликация судов в мультиплеере

### Вопрос GPT

У большого судна может быть много модулей, палуб и настроек. В мультиплеере нужно ли постоянно передавать по сети полное описание типа корабля, либо клиент должен знать статическое описание по `typeId`, а сервер передавать в основном только изменяющееся runtime-состояние?

### Ответ Егора

«Да. Вот так пусть будет. В Multiplayer.»

### Вариант / вывод GPT

Статическое vessel definition не должно дублироваться в каждом сетевом обновлении. Тип судна определяется стабильным `typeId`; клиент и сервер используют соответствующее зарегистрированное definition как общий контракт. По сети реплицируется прежде всего идентичность экземпляра и изменяющееся состояние: мировая позиция и курс, скорость, владелец/экипаж при необходимости, корпус/броня, топливо, состояние модулей, повреждения, локальные позиции находящихся внутри игроков и другие реально изменяемые поля.

Для часто меняющихся данных допускаются компактные дельты/снимки вместо полного runtime-объекта на каждом тике. Полное состояние может отправляться при первоначальном подключении, пересинхронизации или восстановлении после рассинхронизации, но это не означает повторную передачу всего статического описания типа судна.

Архитектура должна сохранять возможность версионировать сетевой/runtime contract, чтобы клиент не интерпретировал `typeId` по несовместимой схеме. Конкретный сетевой код и формат пакетов проектируются при реализации мультиплеерной миграции; сейчас фиксируется принцип разделения статического definition и динамического runtime-state.

### Решение

Подтверждено:

1. В мультиплеере статическое описание типа судна определяется по `typeId` и не должно постоянно передаваться вместе с каждым обновлением.
2. Основной сетевой трафик по судну должен содержать только необходимые изменяющиеся runtime-данные.
3. Для частых обновлений допускается передача компактных дельт вместо полного состояния.
4. Полный runtime snapshot допустим для подключения, явной пересинхронизации или восстановления после рассинхронизации.
5. Статические палубы, геометрия, базовые параметры и другие definition-данные не дублируются в каждом runtime snapshot без необходимости.
6. Network/runtime contract должен быть версионируемым и совместимым с `typeId`-реестром.

---

## Интервью 26. Сервер является источником истины по состоянию судна

### Вопрос GPT

В мультиплеере должен ли сервер быть главным источником истины по состоянию судна — клиент отправляет команды вроде «газ», «поворот», «выстрел», а сервер определяет итоговую позицию, урон, топливо и состояние модулей; при этом клиент может локально предсказывать движение ради меньшей ощущаемой задержки?

### Ответ Егора

«Ну конечно. Конечно. Вот.»

### Вариант / вывод GPT

Мультиплеерная vessel-архитектура должна быть server-authoritative. Клиент передаёт намерение/управляющие команды, а сервер применяет правила физики, боя, расхода ресурсов и изменения модулей и формирует каноническое runtime-состояние судна. Клиентское состояние не считается источником истины для критичных игровых данных.

Для отзывчивого управления клиент может выполнять локальное prediction допустимых действий, прежде всего движения, а затем сверяться с подтверждённым серверным состоянием и при необходимости корректировать расхождение. Это позволяет уменьшить ощущаемую задержку, не передавая клиенту право самостоятельно решать итоговый урон, топливо, состояние корпуса или другие авторитетные параметры.

Конкретная схема prediction/reconciliation и частота сетевых обновлений выбираются при реализации мультиплеерного транспорта. Архитектурный принцип сейчас фиксируется отдельно: авторитет принадлежит серверу, а клиентское предсказание является только способом отображения/управления между подтверждениями.

### Решение

Подтверждено:

1. Сервер является каноническим источником истины по runtime-состоянию судов в мультиплеере.
2. Клиент отправляет команды/намерения управления, а не диктует серверу итоговое состояние судна.
3. Итоговые позиция/скорость, урон, корпус/броня, топливо, состояние модулей и другие критичные данные подтверждаются сервером.
4. Клиентское prediction разрешено для уменьшения ощущаемой задержки, но не меняет серверный авторитет.
5. При расхождении клиент должен корректироваться по серверному состоянию через reconciliation/resync-механизм.
6. Конкретный сетевой алгоритм prediction/reconciliation определяется при реализации и не должен требовать специальных веток по типам конкретных судов.

# Интервью по архитектуре судов

Статус: **интервью не завершено; первая версия архитектурного прототипа частично реализована в `main`.**

Этот документ — живой журнал архитектурного интервью по лодкам, катерам и будущим крупным судам в «Архипелаге».

## Правило работы

- сначала смотрим фактический текущий код и историю проблем;
- затем GPT задаёт один конкретный архитектурный вопрос;
- Егор отвечает;
- после ответа фиксируется вывод/вариант архитектуры;
- неподтверждённое предложение GPT не превращается в требование;
- игровой код меняется только после явного разрешения Егора на соответствующий подтверждённый объём;
- новые решения должны расширять общую архитектуру, а не добавлять специальные `if` для конкретного типа судна;
- если решение ещё не обсуждалось, реализация не должна выдумывать его «на будущее» только ради полноты архитектуры.

## Главный критерий архитектуры

> Новый тип судна, собранный из уже существующих модулей и возможностей, должен добавляться без изменения основного игрового цикла, сонара, аудио, боя, ремонта, посадки/высадки и сетевой репликации.

> Будущее конкретное поведение судна не нужно придумывать заранее. Архитектура обязана дать место для этого поведения, а конкретные правила задаются тогда, когда реально проектируется соответствующее судно.

---

## Интервью 1. Двигатель бронекатера на холостом ходу

### Вопрос GPT

Когда бронекатер стоит с заведённым мотором на месте, каким должен быть звук: постоянный тихий холостой ход без перерывов или мотор должен гудеть только когда катер движется?

### Ответ Егора

«Ну, как у лодки звук, вот как у лодки он должен быть... Ну как бы гудеть его двигатель, чисто вот холостой ход. Ну логично, логично.»

### Решение

Если бронекатер существует, не затонул и двигатель исправен/заведён, двигатель должен звучать постоянно, включая нулевую скорость. Самопроизвольное затухание только из-за отсутствия экипажа недопустимо. Для внешнего слушателя меняются громкость, панорама и фильтрация по реальному положению и расстоянию. При заглохшем/сломавшемся/затонувшем двигателе звук прекращается.

### Реализовано в текущем прототипе

Условие `!occupied && abs(speed) < 0.15`, которое глушило тяжёлый двигатель у пустого стоящего бронекатера, удалено из `updateDualTurretEngine()`. Живой `FreeRoamAudio.updateWorld()` продолжает вызывать этот общий для бронекатера аудиообновитель; бинарный звук не менялся.

---

## Интервью 2. Бронекатер как навигационная цель сонара

### Вопрос GPT

Если игрок сам находится внутри бронекатера, пункт «бронекатер» в навигационных целях должен исчезать или оставаться и сообщать, что игрок уже находится в нём?

### Ответ Егора

«Ну пусть исчезает, типа... Ну а зачем?»

### Решение

Бронекатер должен быть доступен как навигационная цель только игроку, который сейчас не находится внутри этого катера. Если игрок уже находится на/в целевом судне, эта цель не должна засорять ручной список навигации.

### Архитектурный вывод

Не добавлять в сонар специальную ветку только для `dual-turret-patrol`. Будущая система должна уметь получать список навигационно доступных судов через общий признак/возможность и автоматически исключать текущее судно игрока.

### Реализовано в текущем прототипе

Добавлен общий навигационный ID `vessel:<id>` и `vessel-navigation` слой. Ручное меню получает доступные суда через общий список, автоматически исключает `activeBoat` игрока и передаёт серверу общий ID. Сервер каждый тик разрешает этот ID обратно в живое судно и использует его текущие координаты, поэтому цель может двигаться. Знание о том, что старый бронекатер является такой целью, временно изолировано только в legacy-адаптере; generic меню и сценарий не проверяют `dual-turret-patrol`.

---

## Интервью 3. Настройки и фильтрация навигационных целей

### Идея Егора

Сделать полноценные игровые настройки, доступные и с VoiceOver, и с собственной озвучкой игры. В настройках можно будет под себя настраивать озвучку, сонар и другие категории; например скрывать из ручного списка уже хорошо известные цели вроде причала или доски заказов.

### Факт текущего кода

В игре уже существует единый экран настроек. Он открывается из лобби и во время игры, сохраняет значения в `localStorage`, содержит настройки интерфейса, игровой озвучки, скорости речи, автоподключения и динамически добавляемые настройки боевых подсказок.

### Зафиксированный вариант архитектуры

Не создавать второе меню настроек. Расширять существующий центр настроек и со временем разложить его на логические разделы, например:

- Интерфейс;
- Озвучка;
- Сонар и навигация;
- Подсказки;
- Сохранение/подключение.

Фильтрация навигационных целей должна влиять прежде всего на ручной список, не заставляя общий код сонара знать конкретные типы судов.

### Не реализовано пока

Политика фильтров настроек ещё не закончена интервью: отдельно нужно решить, влияет ли скрытие причала/доски только на ручной список или также на миссионное ведение. Поэтому прототип не выдумывает это решение.

---

## Интервью 4. Будущие лодки, корабли и внутренние системы

### Вопрос GPT

Для будущих больших кораблей представляется ли механика так: не просто общий «корпус 700 из 1000», а отдельные повреждаемые системы — двигатель, насос, установка, бомбовый отсек, машинное отделение и т. п., куда игрок может физически добраться и ремонтировать их?

### Ответ Егора

«Да, именно так я представляю.»

Егор уточнил, что архитектура должна быть сделана так, чтобы в будущем появление новых лодок, кораблей, пушек, бомбовых отсеков, палуб и внутренних ремонтируемых частей не требовало переписывать всю игру.

### Зафиксированный вариант архитектуры

Базовой сущностью будущей системы считать не отдельный «бронекатер», а общее **судно (`vessel`)**.

Судно может состоять из следующих слоёв.

### 1. Vessel definition / тип судна

- идентификатор шаблона;
- размеры и базовые параметры;
- физический профиль;
- список зон;
- список модулей;
- точки/места экипажа;
- аудиопрофили;
- навигационные свойства.

### 2. Vessel instance / экземпляр судна

- мировые координаты;
- курс и скорость;
- текущее состояние корпуса/брони;
- текущие экземпляры модулей;
- экипаж;
- груз и другие изменяемые данные.

### 3. Modules / модули

Примеры:

- двигатель;
- насос;
- турель;
- пушка;
- бомбовый отсек;
- сонар;
- генератор;
- ремонтный пост.

Модуль должен описывать своё состояние и предоставляемые возможности, а не заставлять ядро проверять конкретный `boatType`.

### 4. Capabilities / возможности

Общие системы должны работать по возможностям, например:

- `providesPropulsion`;
- `fire`;
- `repairable`;
- `navigationTarget`;
- `producesAudio`;
- `pumpWater`;
- `crewStation`.

Точные имена и полный формат интерфейсов пока не утверждены.

### 5. Zones / зоны и внутреннее пространство

Будущий большой корабль может иметь палубу, рубку, машинное отделение, бомбовый отсек и другие зоны. Игрок может находиться на конкретном судне и одновременно иметь локальную позицию/зону внутри него.

### 6. Generic systems / общие системы

Отдельные общие системы должны обрабатывать:

- физику;
- урон;
- ремонт;
- оружие;
- аудио;
- сонар/навигацию;
- взаимодействие;
- репликацию/сеть.

Эти системы не должны знать названия конкретных судов, если это не загрузка definition, миграция старого сохранения или иной строго изолированный адаптер совместимости.

### Реализовано только как точка расширения

Прототип уже различает definition, runtime instance, modules/capabilities и generic system plugins, но не включает несуществующую сейчас механику ходьбы по палубам, локальных координат и повреждения внутренних отсеков. Эти вещи должны проектироваться отдельными следующими решениями, а не имитироваться заранее.

---

## Интервью 5. Как добавлять новые пушки без переписывания игры

### Вопрос Егора

Как в будущем добавлять новую пушку или другое оборудование так, чтобы не переписывать всю архитектуру?

### Вариант / вывод GPT

Разделить **данные модуля** и **его поведение**.

Если появляется новая разновидность уже существующей пушки, добавляются в основном параметры в definition: урон, дальность, перезарядка, боезапас и т. п. Общий код оружия остаётся тем же.

Если появляется принципиально новый тип механизма (например торпедный аппарат), один раз добавляется новый behavior/handler. После этого любые подходящие суда могут подключать этот модуль декларативно.

Пушка создаёт универсальное описание воздействия (например тип урона, величина, пробитие), а система повреждений сама решает, куда пришёлся удар: броня, корпус, зона или внутренний модуль.

### Архитектурный запрет

Общие системы не должны постепенно превращаться в набор условий вида:

```js
if (boat.boatType === "dual-turret-patrol") { ... }
if (boat.boatType === "new-super-ship") { ... }
```

`boatType`/`vesselType` остаётся идентификатором шаблона для загрузки definition, сохранений и миграций, но не главным механизмом определения поведения.

Архитектурный gate должен блокировать появление новых специальных веток по конкретному `vesselType` в generic `core`, `combat`, `sonar`, `audio`, `repair`, `replication` и аналогичных системах.

---

## Решение Егора о масштабе первой реализации

### Ответ Егора

Сейчас бронекатер должен остаться игровым образом таким же. Полную систему внутренних повреждаемых модулей прямо сейчас внедрять не нужно.

### Решение

Сначала проектируется и документируется расширяемая архитектура так, чтобы текущий бронекатер мог жить в ней без изменения игрового поведения. Детальные повреждаемые внутренние модули, палубы, машинные отделения и другие сложные возможности будут включаться позже без фундаментального переписывания.

Иными словами: **сейчас закладываем интерфейсы и границы системы, но не обязаны сразу активировать все будущие механики.**

---

## Интервью 6. Контент должен подключаться, а не врастать в ядро

### Вопрос GPT

Как удобнее добавлять новый тип судна: через отдельный definition-файл с автоматической регистрацией или через definition-файл плюс явную строку в реестре?

### Ответ Егора

Егор уточнил, что ему не важна программистская форма регистрации сама по себе. Главная цель — сделать архитектуру так, чтобы будущий контент физически не требовал переписывать всю игру и чтобы было трудно нагородить специальные костыли.

Будущие суда могут быть очень крупными: игрок выходит из места управления и ходит по самому кораблю, переходит на палубу, в другие зоны и к конкретным системам. Новые механизмы вроде «мегаядра» или электрошоковой пушки должны создаваться отдельно как модули и затем подключаться к нужным кораблям.

### Вариант / вывод GPT

Контент должен собираться из независимых частей:

- **тип судна** описывает, из каких зон, мест экипажа и модулей оно состоит;
- **зона** описывает локальное пространство судна и связи с соседними зонами;
- **модуль** описывает отдельное оборудование;
- **behavior** реализует новый принцип поведения только тогда, когда такого принципа раньше вообще не существовало;
- **общие системы** обнаруживают модули по стандартным интерфейсам/capabilities и не содержат специальных веток для конкретного корабля.

Пример на человеческом уровне:

1. Отдельно существует модуль «электрошоковая пушка».
2. Он сообщает общей системе оружия необходимые свойства/возможности.
3. В definition корабля сказано только, что в определённой точке/зоне установлен этот модуль.
4. Чтобы поставить ту же пушку на другой корабль, логика пушки не копируется и ядро не меняется.
5. Если игрок идёт по крупному кораблю, его положение может храниться как локальное положение внутри конкретной зоны судна; перемещение самого корабля не требует отдельного мирового перемещения каждой комнаты и каждого модуля.

### Решение

Подтверждено направление: архитектура должна позволять создавать новые модули отдельно и подключать их к любым подходящим судам без копирования логики и без специальных изменений общего ядра. Крупные суда в будущем должны иметь возможность поддерживать внутренние/палубные зоны и физическое перемещение игрока между ними.

Точная техническая форма автоматической регистрации не являлась пользовательским требованием. В прототипе выбраны явные единые entrypoints: content manifest для definition/module content и plugin manifest для фундаментальных систем, чтобы регистрация оставалась проверяемой и не была скрытой магией.

---

## Интервью 7. Не придумывать будущий корабль заранее; гарантировать совместимость заранее

### Вопрос GPT

Когда игрок в будущем отойдёт от штурвала большого корабля, должен ли корабль продолжать идти или автоматически останавливаться?

### Ответ Егора

Егор ответил, что сейчас этого не знает и не хочет заранее придумывать поведение ещё не проектируемого корабля. Когда реально начнётся разработка конкретного корабля, тогда нужно провести отдельное интервью и решить его конкретное поведение.

Главная задача текущей архитектуры другая: будущая новая лодка или корабль должны быстро подключаться к уже существующей игре и сразу «дружить» с общей системой лодок. Не должно повторяться, что новый катер формально существует, но затем выясняется, что на него нельзя нормально сесть, с него нельзя выйти/спрыгнуть, он не работает с сонаром, ремонтом, грузом, буксировкой или другими уже существующими механиками.

### Связь с историей кода

Первый коммит двухместного бронекатера `94a514c4319fb13af0975d5dbef7432418f0917c` называл его модульным, однако содержал отдельные подсистемы и специальные точки интеграции: отдельное состояние покупки `freeDualTurretPurchase`, отдельный `prepareDualTurretBoatRoom`, специальные проверки `boatType === "dual-turret-patrol"` на сервере и отдельную логику выхода `dual-turret-exit`.

Позже коммит `bd71d0ce829bde1a4281611f60b4086061870615` был вынужден перестроить бронекатер как обычную лодку, подчинённую общим законам движения, сонара, груза, ремонта, буксировки, столкновений и выхода. Эта история считается антипримером, который новая архитектура должна предотвращать.

### Вариант / вывод GPT

Нужно разделить **механизм** и **политику конкретного судна**.

Механизм фиксируется сейчас. Он отвечает на вопросы:

- как судно появляется в `world`;
- как оно регистрируется;
- как определяется его физический профиль;
- как игрок садится/входит на него;
- как игрок покидает его;
- как судно участвует в столкновениях и буксировке;
- как сонар получает его координаты и возможность навигации;
- как работают груз, насос, ремонт и топливо, если соответствующие возможности присутствуют;
- как оно получает урон;
- как его состояние реплицируется по сети;
- как аудиосистема получает источники звука;
- как сохраняется и восстанавливается экземпляр судна;
- как будущие зоны и модули могут подключаться к тому же экземпляру.

Политика конкретного судна задаётся позже в его definition/модулях/behavior и отвечает на вопросы:

- сколько мест экипажа;
- какой максимальный ход;
- что происходит без рулевого;
- какие модули установлены;
- есть ли палубы/внутренние зоны;
- можно ли чинить определённый модуль на ходу;
- как конкретное оружие ведёт себя;
- какие особые игровые правила есть у данного судна.

Архитектура не должна заставлять сейчас выбирать эти будущие правила.

### Решение

Подтверждено: конкретное игровое поведение будущего корабля не проектируется сейчас. Оно определяется отдельным интервью при реальном создании этого корабля.

Сейчас архитектура должна гарантировать совместимость с общими системами и возможность добавлять новые свойства без переписывания уже существующего ядра.

### Обязательный compatibility contract — состояние прототипа

Уже заложены и проверяются следующие принципы:

1. новый native-vessel создаётся через единый runtime/factory `spawnVessel()`;
2. definition нормализуется через единый registry;
3. новый native-vessel materializes в совместимый с нынешней игрой runtime boat-объект, а не в параллельный мир сущностей;
4. существующие лодки могут подключаться через sidecar legacy adapter без изменения их сетевого объекта и сохранения;
5. физический профиль использует существующий `free-roam-boat-physics` контракт;
6. generic navigation работает через capability/metadata;
7. новые фундаментальные системы получают generic lifecycle hooks;
8. пользовательские модули обязаны иметь presentation metadata;
9. прямое размножение старых concrete-import/type-check паттернов блокируется архитектурным gate.

Полная проверка всех старых lifecycle-сценариев при миграции конкретной лодки остаётся отдельной стадией и не считается автоматически доказанной только наличием adapter/factory.

---

## Интервью 8. Обязательный vessel factory и «архитектурный компилятор»

### Вопрос GPT

Должно ли вообще любое новое судно обязательно создаваться через одну общую `vessel`-систему, чтобы нельзя было вручную засунуть ещё один объект в `world.boats` в обход архитектуры?

### Ответ Егора

Егор согласился. Дополнительно уточнил, что архитектурные проверки должны быть максимально строгими — «как строгий компилятор»: обход архитектуры должен не просто отмечаться предупреждением, а физически блокировать неправильную реализацию.

Егор также спросил, сколько ещё вопросов потребуется до начала реализации по журналу.

### Решение

Подтверждено:

1. Любое новое судно обязано создаваться и регистрироваться через общий `vessel factory/registry` (точные имена могут измениться).
2. Прямое создание нового игрового судна в обход этого слоя считается архитектурным нарушением.
3. Архитектурные проверки являются **обязательным build gate**: нарушение должно приводить к падению тестов/сборки, а не к предупреждению.
4. Общие системы не должны позволять новому судну подключить собственный параллельный lifecycle посадки, выхода, физики, репликации, урона, аудио или навигации вместо стандартного контракта.
5. Исключения допускаются только в явно выделенных адаптерах миграции/совместимости и сами должны быть ограничены проверками.

### Реализованный вариант «архитектурного компилятора»

В `.github/workflows/vessel-architecture-gate.yml` добавлен отдельный build gate. Он выполняет синтаксическую проверку архитектурных и подтверждённых интеграционных файлов и запускает `tests/vessel-architecture.test.mjs`.

Дополнительно зафиксирован baseline старого архитектурного долга. Новые изменения после baseline проверяются по git diff. Gate запрещает, среди прочего:

- новые прямые `boatType/vesselType === "конкретный-тип"` в обычном новом коде;
- создание/вставку новых лодок прямой мутацией `world.boats` вне разрешённого factory/runtime;
- новые vessel-related source-файлы вне архитектурного пространства расширения;
- прямые импорты concrete definitions/modules/systems в generic-код;
- рост concrete dual-turret зависимостей основного core.

Старый уже существовавший долг не требует немедленно переписывать всю игру, но не должен размножаться.

### Оценка оставшегося интервью

После решений, зафиксированных ниже и частичной реализации прототипа, ориентир сократился примерно до **6–8 существенных архитектурных вопросов**. Это не квота: если один ответ закрывает несколько тем, вопросов будет меньше.

---

## Интервью 9. Пресеты и полностью явное описание возможностей

### Вопрос GPT

Должен ли новый катер автоматически получать стандартный набор возможностей игры, или definition обязан явно перечислять каждую поддерживаемую возможность?

### Ответ Егора

Егор предложил поддерживать **оба варианта**. Если нужен обычный катер и нет смысла подробно перечислять стандартные свойства, можно выбрать готовый пресет. Если нужен необычный тип судна, можно описать всё самостоятельно. Также Егор привёл пример, что конкретному судну может понадобиться запретить буксировку или задать необычную физику.

### Вариант / вывод GPT

Пресет не должен становиться отдельной скрытой веткой игрового поведения. Он должен быть только удобной сокращённой записью.

Например, условный `standard-boat` может разворачиваться в полный канонический набор возможностей: стандартная посадка/выход, столкновения, навигация, буксировка, базовый damage contract, репликация и т. п. После разворачивания движок работает уже не с именем пресета, а с обычным полным нормализованным `vessel definition`.

Полностью ручной definition должен приводиться к точно такому же нормализованному формату и проходить тот же валидатор.

Предпочтительный будущий вариант — разрешить пресету точечные явные переопределения, например «взять `standard-boat`, но `towable: false`». При этом итоговое значение должно быть полностью видно после нормализации и проверяться теми же conformance-тестами.

Необычная физика тоже не должна означать произвольный обход общего vessel-ядра. Судно должно либо выбрать другой зарегистрированный physics profile, либо подключить новый совместимый physics behavior через стандартный интерфейс.

### Решение

Подтверждено и реализовано в базовом registry:

1. Архитектура поддерживает definition через готовый пресет и через полностью явное описание.
2. Оба способа сходятся в один нормализованный vessel contract и проходят одинаковую валидацию.
3. Пресет не получает права обходить архитектурный build gate.
4. `standard-boat` прототип даёт совместимые с текущим runtime базовые поля; точечные capability overrides разрешены.

---

## Интервью 10. Естественная озвучка как обязательная часть контракта модуля

### Вопрос Егора

Как сделать так, чтобы новый модуль — например электрошоковая пушка или Mega Bomb в бомбовом отсеке — автоматически получал правильную естественную озвучку и не порождал фразы вроде «ты сел за электропушка»?

### Ответ Егора

Егор подтвердил, что нормальная озвучка должна быть обязательной частью контракта модуля. В качестве примера: если на судне есть Mega Bomb/бомбовый отсек, игровое событие запуска должно озвучиваться естественно, например по смыслу «Мегабомба запущена из бомбового отсека».

### Связь с текущим кодом

Старый `free-roam-dual-turret-speech.js` знает конкретный тип `dual-turret-patrol` и вручную подменяет тексты событий для бронекатера. Такая схема не масштабируется на десятки будущих модулей и остаётся grandfathered legacy-долгом до миграции этого катера.

### Вариант / вывод GPT

Разделить **смысл игрового события** и **готовую фразу**.

Модуль не должен сам собирать произвольные пользовательские предложения. Он должен публиковать структурированное семантическое событие, например:

- тип события: занято место оператора / запуск / повреждение / ремонт / окончание боезапаса;
- `moduleId`;
- экземпляр/место установки;
- зона судна;
- необходимые числовые параметры или состояние.

Отдельный общий speech/presentation layer преобразует такое событие в естественный текст. Для русского языка модуль/его presentation metadata должны предоставлять достаточную лингвистическую информацию: человекочитаемое название, нужные формы/роли или готовые безопасные фрагменты для поддерживаемых шаблонов. Нельзя полагаться на наивное автоматическое склонение произвольного технического имени.

Один и тот же смысловой текст затем может быть использован собственной TTS игры, экранным текстом и ARIA/VoiceOver, чтобы разные каналы не расходились по смыслу.

Конкретный экземпляр модуля на судне может иметь контекстное имя, например «носовая установка» или «левая электрошоковая установка», не меняя общую механику самого модуля.

### Решение

Подтверждено и заложено в контракт:

1. Пользовательский игровой модуль, который взаимодействует с игроком или создаёт значимые события, обязан иметь корректные speech/presentation metadata.
2. Отсутствие обязательной речевой информации считается ошибкой контракта и должно блокировать build gate.
3. Модуль публикует смысловое событие; естественную фразу формирует общий речевой слой.
4. Технические идентификаторы и сырые имена модулей не должны напрямую попадать в пользовательскую речь.
5. Система должна позволять естественно озвучивать события новых модулей, включая будущие бомбовые отсеки, Mega Bomb и другие механизмы, без добавления специального speech-файла на каждый конкретный тип судна.

В прототипе это реализовано в `vessel-contract.js` и `vessel-presentation.js`: объявленные user-facing semantic events обязаны иметь presentation templates; отсутствие metadata вызывает `VesselContractError`.

---

## Интервью 11. Как добавляется действительно новая механика

### Вопрос Егора

Егор уточнил, что архитектура сама по себе не может «из воздуха» создать будущую механику. Если через время понадобится то, чего сейчас вообще нет в игре — например турбо, новая электрическая система или другой неизвестный механизм — как именно это будет реализовываться?

### Вариант / вывод GPT

Архитектура не должна заранее реализовывать все возможные будущие идеи. Она должна дать им стабильные точки подключения.

Выделены три уровня расширения:

1. **Известное поведение, другие параметры.** Меняются definition/config данные без новой игровой системы.
2. **Новый модуль на существующем фундаменте.** Пишется код только нового module/behavior, а посадка, сеть, presentation, сохранение и другие общие механизмы не переписываются ради него.
3. **Совершенно новый фундаментальный закон игры.** Один раз пишется новая общая system/plugin (например будущая электросеть), после чего любые подходящие суда и модули используют её через контракт.

Главный цикл не должен превращаться в список `updateTurbo()`, `updateReactor()`, `updateElectricGun()` и конкретных кораблей. У него должны быть общие lifecycle phases, а зарегистрированные системы подключаются через них.

### Вопрос GPT

Нужно ли сделать так, чтобы даже совершенно новые общие механики подключались отдельными зарегистрированными `systems/plugins`, а основной игровой цикл не редактировался только ради добавления конкретной новой механики?

### Ответ Егора

«Да, давай так сделаем.»

### Решение

Подтверждено. Новая фундаментальная механика добавляется кодом тогда, когда реально нужна, но подключается через общий system/plugin contract. Нельзя заранее выдумывать саму механику; нельзя и пришивать её новой конкретной веткой к основному core.

### Реализовано в прототипе

- `vessel-plugin-manifest.js` — единая явная точка регистрации фундаментальных vessel systems;
- registry принимает system plugin с ID, lifecycle phase, order и `run(context)`;
- `free-roam-core-v8.js` вызывает generic phases `before-input`, `after-input`, `before-step`, `after-step`;
- сейчас manifest намеренно пуст: несуществующие будущие механики не симулируются заранее.

---

## Разрешение на реализацию и граница текущего прототипа

### Ответ Егора

Егор разрешил реализовать всё, что уже однозначно зафиксировано в журнале, подчеркнув несколько условий:

- строго следовать журналу;
- не придумывать неподтверждённые решения и будущие игровые механики;
- считать нынешнюю архитектуру **прототипом**, а интервью — **незаконченным**;
- существующие лодки должны переноситься в новую архитектуру постепенно и без массового переписывания уже работающего кода;
- архитектурные тесты должны быть максимально строгими, чтобы новые костыли не могли незаметно разрастаться.

### Реализованный migration strategy на этом этапе

Старые лодки не переписаны целиком.

`vessel-legacy-adapter.js` создаёт sidecar-представление существующих объектов через `WeakMap`. Он не добавляет архитектурные поля в сетевой `world`, не меняет формат текущих `world.boats` и не требует миграции сохранения только ради подключения прототипа.

Для новых native-vessels существует обратная сторона моста: `spawnVessel()` создаёт экземпляр через registry/factory, а затем materializes его в совместимый с существующей игрой boat runtime shape. Поэтому старые общие механики могут продолжать работать с привычным `world.boats`, пока внутренний архитектурный слой постепенно становится источником истины.

Это сознательная **переходная архитектура**, а не утверждение, что миграция уже закончена.

---

## Технический checkpoint прототипа в `main`

На момент этой записи уже существуют:

- `public/src/vessel/vessel-contract.js` — строгая схема и ошибки контракта;
- `public/src/vessel/vessel-registry.js` — presets, module types, vessel types, system plugins, normalization и runtime instance factory;
- `public/src/vessel/vessel-defaults.js` — `standard-boat` и legacy preset;
- `public/src/vessel/vessel-runtime.js` — единый native `spawnVessel()`, sidecar runtime и system phases;
- `public/src/vessel/vessel-legacy-adapter.js` — постепенная миграция существующих лодок без изменения их runtime shape;
- `public/src/vessel/vessel-content-manifest.js` — единственная явная точка подключения concrete module/vessel content;
- `public/src/vessel/vessel-plugin-manifest.js` — единственная явная точка подключения новых фундаментальных systems/plugins;
- `public/src/vessel/vessel-presentation.js` — semantic event → естественная пользовательская фраза;
- `public/src/vessel/vessel-navigation.js` — общий список/резолвер навигационных судов;
- generic lifecycle hooks в `free-roam-core-v8.js`;
- strict architecture gate и baseline старого долга;
- подтверждённый непрерывный холостой ход двигателя бронекатера;
- подтверждённая generic движущаяся sonar-цель судна с автоматическим исключением собственного текущего судна;
- Safari cache-busters для изменённого графа модулей.

### Что этот checkpoint НЕ утверждает

- существующие стандартная лодка и бронекатер ещё не переписаны как полноценные native definitions/modules;
- walkable zones/палубы и vessel-local player coordinates ещё не реализованы;
- многодвигательная тяга, асимметричная тяга, турбо и другие приведённые примеры не являются текущими требованиями и не реализованы;
- система внутренних повреждаемых модулей ещё не включена в текущий бронекатер;
- окончательная save/network schema versioning и migration policy ещё не выбраны;
- окончательный контракт нестандартного physics behavior ещё не выбран;
- настройки фильтрации навигационных целей ещё не закончены;
- наличие архитектурного моста само по себе не доказывает все будущие compatibility-сценарии: каждый реально мигрируемый тип нужно проверять его conformance-сценариями.

---

## Формат всех следующих записей

Для каждого следующего вопроса продолжать в таком виде:

### Вопрос GPT
Точный вопрос, заданный Егору.

### Ответ Егора
Ответ Егора максимально близко к смыслу его формулировки; важные прямые формулировки можно сохранять в кавычках.

### Вариант / вывод GPT
Как ответ переводится в архитектурное решение; какие варианты предлагаются; что остаётся открытым.

### Решение
Только то, что действительно подтверждено Егором. Неподтверждённые предложения не превращать в требования.

### Связь с текущим кодом
При необходимости фиксировать, где текущая реализация совпадает или расходится с решением.

---

## Открытые вопросы после первого implementation checkpoint

Ориентир: ещё примерно **6–8 существенных вопросов**, часть может закрыться одним ответом.

1. Как версионировать runtime/save schema судов и как безопасно мигрировать старые миры, когда конкретная лодка станет native-vessel?
2. Какой точный контракт compatibility/mount/slot нужен модулю: что должно проверяться жёстко, а что разрешается явно расширять для необычных механизмов?
3. Как хранить будущие зоны и vessel-local координаты игрока так, чтобы маленькие лодки не получили ненужную сложность?
4. Какой контракт должен иметь новый physics behavior, если стандартного `physicsProfile` недостаточно?
5. Как именно переносить нынешние стандартную лодку и бронекатер с legacy bridge на native definitions поэтапно, не меняя их игрового поведения одним большим переписыванием?
6. Какой минимальный демонстрационный «третий катер» позже использовать как доказательство: existing modules + новый definition, без изменения core/sonar/audio/repair?
7. Как должна работать политика настроек ручного списка сонара относительно миссионного ведения?
8. Нужны ли дополнительные build-gate правила после первой практической попытки добавить новый module/system через эти extension points?
